/*Ваша программа принимает от пользователя индекс (позицию) и текст. Она должна записать этот текст в файл storage.bin по указанной позиции.
Ситуация: Пользователь отправляет позицию 10 МБ, а файл на диске сейчас весит всего 1 МБ.
Задание: Напишите последовательность системных вызовов, которая позволит записать данные на позицию 10 МБ,
НЕ ЗАПОЛНЯЯ нулями весь промежуток между 1 МБ и 10 МБ. Объясните, зачем это нужно в практической разработке (чем это лучше, чем запись в конец)?*/


#include <fcntl.h>
#include <unistd.h>

int fd = open("storage.bin", O_WRONLY | O_CREAT, 0644);
if (fd < 0) {
}

// перемещаем указатель на позицию 10 МБ (10 * 1024 * 1024)
off_t pos = 10 * 1024 * 1024;
off_t result = lseek(fd, pos, SEEK_SET);
if (result == (off_t)-1) {
}

// записываем текст 
ssize_t bytes_written = write(fd, user_text, text_len);
if (bytes_written < 0) {
}

// закрываем файл
close(fd);
